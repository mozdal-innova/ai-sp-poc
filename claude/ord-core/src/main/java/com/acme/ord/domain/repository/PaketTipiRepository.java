package com.acme.ord.domain.repository;

import com.acme.ord.domain.entity.PaketTipi;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaketTipiRepository extends JpaRepository<PaketTipi, Long> {
}
